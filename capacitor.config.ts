import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'ondevice-shopping-assistant',
  webDir: 'dist'
};

export default config;
